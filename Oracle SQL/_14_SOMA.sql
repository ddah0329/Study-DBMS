CREATE TABLE SOMA(
	NAME VARCHAR2(500),
	ID VARCHAR2(500),
	SALARY NUMBER,
	BRANCH_ID NUMBER
);

INSERT INTO SOMA
	VALUES ('def', 'no4', 280, 16);

ALTER TABLE SOMA 
	ADD(TOTAL NUMBER);

ALTER TABLE SOMA 
	DROP COLUMN TOTAL;

SELECT * FROM SOMA;
	
SELECT DISTINCT 
	BRANCH_ID, 
	TOTAL 
FROM 
	SOMA
ORDER BY
	BRANCH_ID;
	
SELECT 
	BRANCH_ID, 
	SUM(SALARY) TOTAL
FROM 
	SOMA
GROUP BY 
	BRANCH_ID
ORDER BY 
	BRANCH_ID;
